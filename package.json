{
  "name": "@f8n-ai/semdag",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "bun build ./index.ts --outdir ./dist --target node && bun build ./index.ts --outdir ./dist --declaration",
    "clean": "rm -rf dist",
    "prepublishOnly": "bun run clean && bun run build",
    "check": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "lefthook": "^1.10.10",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "trustedDependencies": ["@biomejs/biome", "lefthook"],
  "engines": {
    "bun": "1.2.2"
  }
}
